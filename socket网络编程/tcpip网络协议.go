package main
func main(){
/* tcp/ip协议族,来自于OSI标准七层协议,,概念分为四层(就像是发快递捡包裹一样) :
第一层 : 应用层
解析处理数据 (收发,分类包裹)
第二层 : 传输层
解析一个ip下一个单独的应用端口地址(哪个省)
第三层 : 网络层(网间层)
解析一个mac网卡地址下的单独的IP地址(哪个市)
第四层 : 链路层
解析一个物理网卡下的单独的mac地址(哪个县)
原则 :
上层建立在下层之上,下层为上层提供支持
 */
}